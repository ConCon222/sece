name: Daily CFP Scraper

on:
  schedule:
    # 每天 UTC 时间 00:00 运行 (北京时间早上 8:00)
    - cron: '0 0 * * *'
  workflow_dispatch: # 允许手动点按钮触发

permissions:
  contents: write

jobs:
  scrape-and-update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      # 安装 Chrome 浏览器 (DrissionPage 需要)
      - name: Install Chromium Browser
        run: |
          sudo apt-get update
          sudo apt-get install -y chromium-browser

      - name: Install dependencies
        run: |
          pip install curl-cffi DrissionPage beautifulsoup4 pyyaml lxml

      - name: Run Scraper
        run: |
          # 确保脚本有执行权限（可选）
          # 假设你的脚本在根目录，且输出到 _data/cfps.yml
          # 你需要根据实际路径修改下面的路径
          python a-test.py 
        env:
          # 如果你的脚本里写死了路径，这里不需要改；
          # 建议在脚本里把 OUTPUT_YML_PATH 改为 relative path 如 '_data/cfps.yml'
          PYTHONUNBUFFERED: 1

      - name: Check for changes
        id: git-check
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'
          git add _data/cfps.yml
          # 检查是否有变动，有变动才提交
          git diff --staged --quiet || echo "changes=true" >> $GITHUB_OUTPUT

      - name: Commit and Push
        if: steps.git-check.outputs.changes == 'true'
        run: |
          git commit -m "🤖 Auto-update CFP list [$(date +'%Y-%m-%d')]"
          git push